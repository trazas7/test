import com.hazelcast.config.Config;
import com.hazelcast.config.JoinConfig;
import com.hazelcast.config.NetworkConfig;
import com.hazelcast.spi.discovery.DiscoveryStrategyConfig;
import com.hazelcast.eureka.one.EurekaOneDiscoveryStrategyFactory;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.HashMap;
import java.util.Map;

@Configuration
public class HazelcastConfig {

    @Bean
    public Config hazelcastConfig() {
        Config config = new Config();

        // Network and Join Configuration
        NetworkConfig networkConfig = config.getNetworkConfig();
        JoinConfig joinConfig = networkConfig.getJoin();

        // Disable multicast, TCP/IP, AWS
        joinConfig.getMulticastConfig().setEnabled(false);
        joinConfig.getTcpIpConfig().setEnabled(false);
        joinConfig.getAwsConfig().setEnabled(false);

        // Configure EurekaOneDiscoveryStrategy without using eureka-client.properties
        Map<String, Comparable> discoveryStrategyProperties = new HashMap<>();
        discoveryStrategyProperties.put("self-registration", "true");
        discoveryStrategyProperties.put("namespace", "hazelcast");
        discoveryStrategyProperties.put("use-metadata-for-host-and-port", "true");

        // Set Eureka properties directly without eureka-client.properties
        discoveryStrategyProperties.put("eureka.serviceUrl.default", "http://<your-eureka-server-url>/eureka/");
        discoveryStrategyProperties.put("eureka.prefer-ip-address", "true");
        discoveryStrategyProperties.put("eureka.instance.hostname", "hazelcast-instance");

        DiscoveryStrategyConfig discoveryStrategyConfig = new DiscoveryStrategyConfig(
            new EurekaOneDiscoveryStrategyFactory(), discoveryStrategyProperties);

        joinConfig.getDiscoveryConfig().addDiscoveryStrategyConfig(discoveryStrategyConfig);

        return config;
    }
}
