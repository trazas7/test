solution:

```java
@Query("SELECT b FROM BatchSubscription b WHERE b.topicName LIKE 'NCN%' " +
       "AND (b.createDate IS NULL OR b.createDate >= :dayAgo)")
List<BatchSubscription> findNCNBatchSubscriptionsLast24Hours(
    @Param("dayAgo") LocalDateTime dayAgo);
```

To use this:

```java
@Repository
public interface BatchSubscriptionRepository extends JpaRepository<BatchSubscription, Long> {
    @Query("SELECT b FROM BatchSubscription b WHERE b.topicName LIKE 'NCN%' " +
           "AND (b.createDate IS NULL OR b.createDate >= :dayAgo)")
    List<BatchSubscription> findNCNBatchSubscriptionsLast24Hours(
        @Param("dayAgo") LocalDateTime dayAgo);
}

// In your service:
LocalDateTime dayAgo = LocalDateTime.now().minusDays(1);
List<BatchSubscription> subscriptions = repository.findNCNBatchSubscriptionsLast24Hours(dayAgo);
```

Alternatively, you could use method naming convention:

```java
List<BatchSubscription> findByTopicNameStartingWithAndCreateDateIsNullOrCreateDateGreaterThanEqual(
    String prefix, LocalDateTime dayAgo);
