
import sanitizeHtml from "sanitize-html";

export function minimalize(html) {
  return sanitizeHtml(html, {
    allowedTags: ["a", "br"],
    allowedAttributes: { a: ["href"] },
    transformTags: {
      p: "br", div: "br", li: "br", tr: "br", h1: "br", h2: "br", h3: "br"
    }
  }).trim();
}
