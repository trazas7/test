// npm i jszip
import JSZip from "jszip";

/**
 * Selected text → selected text box → if a TABLE is selected,
 * parse ONLY that table on the active slide by matching the shape name.
 */
export async function getPptSelectedTextOrShape(
  allowFallbackAllSlides = false // keep false to avoid “whole deck” parsing
): Promise<string> {
  const t0 = performance.now();
  console.log("🚀 [getPptSelectedTextOrShape] start");

  let activeSlideIndex: number | null = null; // 1-based on most hosts
  let selectedTableName: string | null = null;

  const immediateText = await PowerPoint.run(async (ctx) => {
    try {
      console.log("🔎 step 1: try highlighted text");
      const sel = ctx.presentation.getSelectedTextRangeOrNullObject();
      await ctx.sync();
      if (!sel.isNullObject) {
        sel.load("text");
        await ctx.sync();
        const t = (sel.text ?? "").trim();
        console.log("🖱️ selected text:", JSON.stringify(t));
        if (t) return t;
      } else {
        console.log("ℹ️ no selected text range");
      }

      console.log("🔎 step 2: inspect selected shapes");
      const shapes = ctx.presentation.getSelectedShapes();
      shapes.load("items");
      await ctx.sync();
      console.log("📦 selected shapes:", shapes.items.length);

      if (shapes.items.length > 0) {
        // Load only *valid* props for PowerPoint.Shape
        shapes.items.forEach((s) => s.load("type,name"));
        await ctx.sync();

        for (const s of shapes.items as any[]) {
          console.log(`• shape name=${s.name} type=${s.type}`);

          // If a text box is selected, load its text lazily
          if (s.type === "TextBox") {
            try {
              s.load("textFrame/textRange/text");
              await ctx.sync();
              const txt = s?.textFrame?.textRange?.text?.trim();
              if (txt) {
                console.log("📝 returning textbox text");
                return txt;
              }
            } catch (e) {
              console.log("⚠️ failed to read textbox text:", e);
            }
          }

          if (s.type === "Table" && !selectedTableName) {
            selectedTableName = s.name ?? null;
            console.log("📊 table selected; name:", selectedTableName);
          }
        }
      } else {
        console.log("ℹ️ no shapes selected");
      }

      // Capture active slide index (best-effort)
      try {
        const slide = ctx.presentation.slides.getActiveSlide();
        slide.load("index");
        await ctx.sync();
        activeSlideIndex = (slide as any).index ?? null;
        console.log("📍 active slide index:", activeSlideIndex);
      } catch (e) {
        console.log("⚠️ could not read active slide index:", e);
      }

      return "";
    } catch (err) {
      console.error("💥 error during selection scan:", err);
      return "";
    }
  });

  if (immediateText?.trim()) {
    console.log(
      `✅ plain text path in ${Math.round(performance.now() - t0)} ms`
    );
    return immediateText.trim();
  }

  // If we don’t know which table/slide to target, don’t parse the whole deck unless allowed
  if (!selectedTableName || !activeSlideIndex) {
    console.log(
      "⛔ no selected table or slide index; not parsing entire deck",
      { selectedTableName, activeSlideIndex, allowFallbackAllSlides }
    );
    if (!allowFallbackAllSlides) {
      return "Select a single table, then try again (I only parse the selected table).";
    }
  }

  // ZIP parsing with strict filters
  console.log(
    "🧭 going to ZIP parse with filters:",
    JSON.stringify({ activeSlideIndex, selectedTableName, allowFallbackAllSlides })
  );

  try {
    const tZip0 = performance.now();
    const tsv = await readPptTablesAsTSV({
      preferSlideIndex: activeSlideIndex ?? null,
      filterShapeName: selectedTableName ?? null,
      fallbackAllSlides: !!allowFallbackAllSlides,
    });
    console.log(
      `📦 ZIP parse finished in ${Math.round(performance.now() - tZip0)} ms`
    );
    if (tsv.trim()) {
      console.log(
        `✅ returning selected table TSV in ${Math.round(
          performance.now() - t0
        )} ms`
      );
      return tsv;
    }
    console.log("❌ no TSV found for the selected table/slide");
  } catch (zipErr) {
    console.error("💥 ZIP parse failed:", zipErr);
  }

  return "";
}

/* ------------------------------------------------------------------------------------------------
 * ZIP parsing with shape-name filter (matches <p:cNvPr name="…"> inside graphicFrame on the slide)
 * ------------------------------------------------------------------------------------------------*/

type ReadTablesOpts = {
  preferSlideIndex?: number | null;       // 1-based slide index
  filterShapeName?: string | null;        // match <p:cNvPr name="…">
  fallbackAllSlides?: boolean;            // if true, roam all slides when preferred not found
};

export async function readPptTablesAsTSV(
  opts: ReadTablesOpts = {}
): Promise<string> {
  const {
    preferSlideIndex = null,
    filterShapeName = null,
    fallbackAllSlides = false,
  } = opts;

  console.log(
    "🔧 [readPptTablesAsTSV] opts:",
    JSON.stringify({ preferSlideIndex, filterShapeName, fallbackAllSlides })
  );

  console.log("⬇️ fetching PPTX ZIP");
  const zipBuffer = await getPptxZipArrayBuffer();
  console.log("✅ ZIP bytes:", (zipBuffer.byteLength / 1024).toFixed(1), "KB");

  console.log("🧵 unzipping...");
  const zip = await JSZip.loadAsync(zipBuffer);

  const slidePaths = Object.keys(zip.files)
    .filter((p) => /^ppt\/slides\/slide\d+\.xml$/.test(p))
    .sort((a, b) => {
      const na = parseInt(a.match(/slide(\d+)\.xml/)![1], 10);
      const nb = parseInt(b.match(/slide(\d+)\.xml/)![1], 10);
      return na - nb;
    });

  console.log("📄 slide XMLs:", slidePaths.length);

  // Target slide(s)
  let targets: string[] = slidePaths;
  if (preferSlideIndex) {
    const specific = slidePaths.filter((p) =>
      p.endsWith(`slide${preferSlideIndex}.xml`)
    );
    if (specific.length) {
      targets = specific;
      console.log("🎯 targeting only:", targets[0]);
    } else if (!fallbackAllSlides) {
      console.log("⛔ preferred slide not found; not falling back");
      return "";
    } else {
      console.log("↪️ preferred slide not found; falling back to all slides");
    }
  } else if (!fallbackAllSlides) {
    console.log("⛔ no preferred slide provided; not falling back");
    return "";
  }

  let out: string[] = [];
  for (const path of targets) {
    console.log("🧩 parse", path);
    const xmlText = await zip.files[path].async("text");
    const tsvBlocks = extractTablesFromSlideXML(xmlText, filterShapeName);
    console.log(
      `   📊 tables returned from ${path}:`,
      tsvBlocks.length,
      "(filtered by name:",
      filterShapeName,
      ")"
    );
    out.push(...tsvBlocks);

    // If filtering by name and we found the match, we can stop.
    if (filterShapeName && tsvBlocks.length > 0) break;
  }

  const result = out.join("\n\n").trim();
  console.log("🧾 final TSV length:", result.length);
  return result;
}

/** stream the current presentation into memory as a ZIP ArrayBuffer */
async function getPptxZipArrayBuffer(): Promise<ArrayBuffer> {
  const file = await getFileAsync(Office.FileType.Compressed);
  console.log("📦 file acquired; slices:", file.sliceCount);
  try {
    const chunks: Uint8Array[] = [];
    for (let i = 0; i < file.sliceCount; i++) {
      const slice = await getSliceAsync(file, i);
      const u8 = new Uint8Array(slice.value.data as number[]);
      chunks.push(u8);
      if (i === 0 || i === file.sliceCount - 1 || (i + 1) % 10 === 0) {
        console.log(`   🔹 slice ${i + 1}/${file.sliceCount} (${u8.length} B)`);
      }
    }
    const total = chunks.reduce((s, u) => s + u.length, 0);
    console.log("📏 total bytes:", total);
    const merged = new Uint8Array(total);
    let off = 0;
    for (const u of chunks) {
      merged.set(u, off);
      off += u.length;
    }
    return merged.buffer;
  } finally {
    file.closeAsync(() => console.log("🧹 file closed"));
  }
}
function getFileAsync(type: Office.FileType): Promise<Office.File> {
  return new Promise((resolve, reject) => {
    Office.context.document.getFileAsync(type, (res) => {
      if (res.status === Office.AsyncResultStatus.Succeeded) resolve(res.value);
      else {
        console.error("❌ getFileAsync failed:", res.error);
        reject(res.error);
      }
    });
  });
}
function getSliceAsync(file: Office.File, index: number) {
  return new Promise<Office.AsyncResult<Office.Slice>>((resolve, reject) => {
    file.getSliceAsync(index, (res) => {
      if (res.status === Office.AsyncResultStatus.Succeeded) resolve(res);
      else {
        console.error(`❌ getSliceAsync(${index}) failed:`, res.error);
        reject(res.error);
      }
    });
  });
}

/* --------------------------- XML helpers (with filtering) --------------------------- */

/**
 * If filterShapeName is provided, find <p:graphicFrame> whose
 * <p:nvGraphicFramePr><p:cNvPr name="filterShapeName"> and extract its <a:tbl>.
 * Otherwise (and only if allowed by caller), extract all tables on the slide.
 */
function extractTablesFromSlideXML(
  xml: string,
  filterShapeName: string | null
): string[] {
  console.log(
    "🔬 parse slide XML (length:",
    xml.length,
    ") filterShapeName=",
    filterShapeName
  );
  const doc = new DOMParser().parseFromString(xml, "application/xml");

  if (filterShapeName) {
    // Match graphicFrame by cNvPr@name
    const frames = Array.from(doc.getElementsByTagNameNS("*", "graphicFrame"));
    console.log("🔎 graphicFrame count:", frames.length);

    for (const gf of frames) {
      const nv = firstChildByLocalName(gf, "nvGraphicFramePr");
      const cNvPr = nv ? firstChildByLocalName(nv, "cNvPr") : null;
      const name = cNvPr?.getAttribute("name") ?? "";
      if (name === filterShapeName) {
        console.log("🎯 matched graphicFrame by name:", name);
        const graphic = firstChildByLocalName(gf, "graphic");
        const graphicData = graphic
          ? firstChildByLocalName(graphic, "graphicData")
          : null;
        const tbl = graphicData ? firstChildByLocalName(graphicData, "tbl") : null;
        if (!tbl) {
          console.log("⚠️ matched frame has no <tbl> inside");
          return [];
        }
        return [tableElementToTSV(tbl, 0)];
      }
    }
    console.log("⚠️ no graphicFrame matched by name; returning []");
    return [];
  }

  // No filter → caller should have allowed fallback; still log
  const tables: Element[] = [...Array.from(doc.getElementsByTagNameNS("*", "tbl"))];
  console.log("🔎 <tbl> count (no filter):", tables.length);
  return tables.map((t, i) => tableElementToTSV(t, i)).filter((s) => s.trim());
}

function firstChildByLocalName(parent: Element, local: string): Element | null {
  for (let i = 0; i < parent.childNodes.length; i++) {
    const n = parent.childNodes[i];
    if (n.nodeType === Node.ELEMENT_NODE && (n as Element).localName === local) {
      return n as Element;
    }
  }
  return null;
}

/** Convert <a:tbl> to TSV and log cell contents */
function tableElementToTSV(tblEl: Element, tblIndex: number): string {
  const rows = Array.from(tblEl.getElementsByTagNameNS("*", "tr"));
  console.log(`🧱 table#${tblIndex} rows:`, rows.length);

  const lines: string[] = [];
  for (let r = 0; r < rows.length; r++) {
    const tr = rows[r];
    const cells = Array.from(tr.getElementsByTagNameNS("*", "tc"));
    const vals = cells.map((tc, c) => {
      const t = getTextFromCell(tc);
      console.log(`   • r${r}c${c} text=`, JSON.stringify(t));
      return t;
    });

    // trim trailing empties
    let end = vals.length;
    while (end > 0 && (!vals[end - 1] || !vals[end - 1].trim())) end--;
    lines.push(vals.slice(0, end).join("\t"));
  }
  while (lines.length && !lines[lines.length - 1].trim()) lines.pop();

  const tsv = lines.join("\n");
  console.log(`🧾 table#${tblIndex} TSV length:`, tsv.length);
  return tsv;
}

function getTextFromCell(tc: Element): string {
  const doc = tc.ownerDocument!;
  const walker = doc.createTreeWalker(
    tc,
    NodeFilter.SHOW_ELEMENT | NodeFilter.SHOW_TEXT
  );

  let n: Node | null = walker.nextNode();
  let out = "";
  while (n) {
    if (n.nodeType === Node.ELEMENT_NODE) {
      const el = n as Element;
      if (el.localName === "t") out += el.textContent ?? "";
      else if (el.localName === "br") out += "\n";
    }
    n = walker.nextNode();
  }
  return out.replace(/\s+\n/g, "\n").replace(/\n{3,}/g, "\n\n").trim();
}